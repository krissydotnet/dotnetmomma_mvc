<div class="pull-right">
    <a href="@Url.Action("Edit", new { id = ViewData["id"]})" class="btn btn-success btn-xs margin-right">
        <span class="fas fa-pencil-alt"></span><span class="hidden-xs">Edit</span>
    </a>
    <a href="@Url.Action("Details", new { id = ViewData["id"]})" class="btn btn-primary btn-xs margin-right">
        <span class="fas fa-info-circle"></span><span class="hidden-xs"> Details</span>
    </a>
    <a href="@Url.Action("Delete", new { id = ViewData["id"]})" class="btn btn-danger btn-xs margin-right">
        <span class="fas fa-trash"></span><span class="hidden-xs"> Delete</span>
    </a>
</div>
